extends layout

block content
  h1= title
  p Welcome to #{title}

  form(action="/login/uploadFile", method="post", enctype="multipart/form-data")
    input(type="file", name="sampleFile")
    input(type="submit", value="upload")